# DEMO — 시연 대본 · 트랙 · 가드레일

장면 원천: `ssot/scenarios.yaml demo[]`(9/30 시연시나리오 rev02 10장면) → 생성 표 [generated/DEMO.md](generated/DEMO.md)(장면 ↔ 화면·조작·메시지·ACC·픽스처). 이 문서는 대본·순서·준비만 적는다.

## 1. 서사

기능 나열이 아니라 **고장 하나가 처리되는 전 과정**을 따라간다. 원본 고객 애로("원인 파악 어려움 → 출동 → 지연", "안전관리자 대응 전담 인력", "실시간 모니터링 불가")가 장면마다 해소된다. 약 25분.

| 순서 | 장면 | 트랙 | 한 줄 |
|---|---|---|---|
| 1 | 상황 발생 (B1-02) | D1 | CPB-003 전압 이상이 관제 화면에 뜬다 |
| 2 | 운전자 인지 (A2-02 · A2-03) | D1 | 운전자도 같은 알림을 받고 체크인·점검을 한다 |
| 3 | 현장 접수 (A1-02 → A1-03 → A1-04/05) | D1 | 안전관리자가 업무를 접수하고 영상을 본다 |
| 4 | 원격 진단·배정 (B1-02 · B1-03) | D1 | 관제가 고장코드로 원인을 알고 정비를 보낸다 |
| 5 | 완료·증빙 (A1-03 → A1-08 → A1-06) | D1 | 현장이 완료를 확인하고 이력이 남는다 |
| 6 | 에스컬레이션 (B1-04 · A3-02 · B2-02/03) | D1 | 방치된 문제가 1시간 뒤 본사에 통보된다 |
| 7 | 서류 흐름 (A2-05 → A1-02) | D4 | 서류가 앱에서 완결된다 |
| 8 | 확장성 (B4-02) | D4 | 프로토콜 업로드·오류 파싱·알림 |
| 9 | 사업 가치 (B1-06) | D4 | 만료 임박 계약과 재배치 |
| 10 | 쇼케이스 (B3-06 · B1-07) | D5 | 읽기 전용·마스킹된 보여주기 화면 |

## 2. 트랙 (준비 패키지)

| 트랙 | 목적 | 데이터 | 웨이브 |
|---|---|---|---|
| D1 데이터 인입 → 업무 스파인 (필수) | 장비가 보내는 것이 화면에 뜨고 업무로 흐른다 | 실 2set 또는 리플레이 에이전트 · 스파인 mock | W1 mock → W3 실 |
| D2 영상 옵션 | 카메라 2대가 관제에 붙고 이벤트가 업무가 된다 · 장애 시 "AI 판단 불가" | 라이브 1ch 실(가능 시) + AI ch 녹화 클립·bbox · E1 POST 시뮬레이터 | W1 mock → W3 |
| D3 사업주 모니터링 (2단계 미리보기) | 투입 요청 → 가용 호기 → 배정 → 운전자 배치 | mock | W4 |
| D4 CraneEyes 공통 | 수신함·권한·프로토콜·임계·서류·임대 | mock + YAML 파싱 실 | W1~W2 |
| D5 프로파일·바디캠·개인정보 | 현장마다 달라도 한 플랫폼 | 프리셋 3(P-LITE/P-SD/P-NVR) 전환 mock · 동의 배지 · 마스킹 | W2·W4 |

## 3. 준비 체크리스트 (시연 30분 전)

프로토타입 2set 전원·LTE · CCTV 스트림 수신 · 데모 계정 로그인 상태(`roles.yaml`) · 관제 WEB 유선 · **리플레이 폴백**(통신 장애 시 녹화 스트림·리플레이 에이전트로 전환, 사전 공지) · 리셋 = `?state=` 초기 픽스처로 새로고침 · DemoClock "1시간 경과" 점프 준비.

## 4. 가드레일 (만들지 않는 것)

HLS/WebRTC 중계·RTSP·VPN · MQTT/SSE 서버 · 실인증 · S3 업로드 · 바디캠 실연동 · Mapbox 토큰 → 대신 루프 MP4 2클립 + 스냅샷 모드 + bbox SVG · DemoClock 타임라인 재생 · 역할 카드 로그인 + 데모 계정 · objectURL 미리보기 · 세션 카드·동의 배지·CSS 블러 · MapLibre+CARTO. **진짜로 만드는 것**: YAML/JSON 파싱·오류 판정 · 업무 상태기계 · 프로파일→피처플래그 · 스코프별 라우트 가드.

## 5. 리플레이 에이전트 (W1 · specs/demo-scripts)

`?scene=N`으로 장면에 진입하면 장면 계정 세션·장면 픽스처(`packages/mock/src/demo.ts`)·타임라인이 적용되고, 셸 하단 장면 바에서 이전/다음 장면으로 이동한다(앱이 바뀌면 절대 URL — `PUBLIC_WEB_URL`/`PUBLIC_PWA_URL`, 기본 preview 4173/4174). 실 인입(W3)이 붙어도 폴백으로 유지.

| 장면 | 진입 URL(preview) | 계정 | 재생 |
|---|---|---|---|
| 1 | `web/b1/dash?scene=1` | control01 | 진입 시 CPB-003 정상 → 3초 뒤 E-021 긴급 알림 · fault 전환 · C-105 발행 |
| 2 | `pwa/a2/today?scene=2` | driver03 | 알림 → 체크인 → 점검 제출(같은 장면 안에서 db 유지) |
| 3 | `pwa/a1/inbox?scene=3` | safety01 | C-105 접수 → 영상(A1-04/05) → 정비 호출 |
| 4 | `web/b1/dash?scene=4` | control01 | C-105 접수됨 상태 · `?case=C-105` 패널 |
| 5 | `pwa/a1/inbox/C-105?scene=5` | safety01 | 정비 호출 이력 → 완료 확인 시트(조치 내용 필수) → 기록 탭(A1-06) 최상단에 완료 확인 행 |
| 6 | `web/b1/escalation?scene=6` | control01 | CPB-004 55분 방치 → 장면 바 "1시간 경과" → 본사·관제 통보 |
| 7 | `pwa/a2/docs?scene=7` | driver03 | DOC-001 만료 임박 D-27 → 촬영·제출 → 검토 중(수신함·현장 업무함에 서류 업무) |
| 8 | `web/b4/protocols?scene=8` | ops01 | YAML 업로드 · 오류 샘플 · 알림 미리보기 |
| 9 | `web/b1/leases?scene=9` | control01 | LS-001 D-27 만료 임박 최상단 → 재배치 계획(대상 현장 · 메모) → relocated · 이력 · 안전관리자 홍보 연계 문구 |
| 10 | `web/b1/showcase?scene=10` | safety01 → control01 대체(B1-07 권한) | 다크 강제 쇼케이스 · 무사고 D+ · 점검·서류 지표 · 카메라 월 · 마스킹(DISC-031) · Esc/클릭 → B1-02 |

리셋 = 같은 `?scene=N`으로 새로고침(장면 db는 세션 캐시 키 `scene|N`).

## 6. 확정 필요 (DISC-017)

시연 일시·장소·참석자 · 실장비 2set 장착 상태(제어기·CCTV) · 시연장 네트워크 · 데모 데이터(실 고장코드 E-021 반영 여부) · 일정 재기준선(`docs/PLAN.md`).
