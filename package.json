{
  "name": "boomeyes-owner",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node tools/check-handlers.mjs && tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "node tools/check-handlers.mjs && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
