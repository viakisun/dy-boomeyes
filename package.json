{
  "name": "boomeyes-app",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "ssot:check": "node tools/ssot/check.mjs",
    "ssot:build": "node tools/ssot/build.mjs",
    "tokens:check": "node packages/tokens/scripts/check.mjs",
    "tokens:build": "node packages/tokens/scripts/build.mjs",
    "verify": "pnpm ssot:check && pnpm tokens:check && pnpm ssot:build && pnpm tokens:build && git diff --exit-code -- docs/generated packages/domain/src/generated packages/tokens/dist"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  }
}
